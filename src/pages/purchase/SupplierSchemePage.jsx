// The purchase-menu entry intentionally shares the authoritative supplier scheme
// workbench so no legacy caller-controlled claim or achievement flow remains.
export { default } from '../schemes/SupplierSchemeEntry.jsx';
